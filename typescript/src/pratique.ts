let num1: number = 1.5;
let num2: number = 2;

const multiplica = (num1: number, num2 : number): number => num1 * num2;

console.log(multiplica(num1,num2))

const nomeQualquer: string = "João"

const fraseDeSaudacao = (nome: string): string => "Olá, " + nome;

console.log((fraseDeSaudacao(nomeQualquer)));